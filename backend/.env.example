# ======================================
# ESNAF KURYE BACKEND — ORTAM DEĞİŞKENLERİ
# Bu dosya şablondur. .env olarak kopyalayıp değerleri doldurun:
#   cp .env.example .env
# !! .env dosyası asla commit edilmemelidir !!
# ======================================

# Sunucu
PORT=3000
NODE_ENV=development

# PostgreSQL — PostGIS extension gerekli
DB_HOST=localhost
DB_PORT=5432
DB_NAME=esnaf_kurye
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT — ZORUNLU: Ayarlanmazsa sunucu başlamaz
JWT_SECRET=your_super_secret_key_change_this
JWT_EXPIRES_IN=7d

# CORS — Virgülle ayrılmış izinli origin'ler (opsiyonel)
# Ayarlanmazsa tüm origin'lere izin verilir (sadece development)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Hava Durumu API (OpenWeatherMap) — Opsiyonel
# Ayarlanmazsa hava çarpanı her zaman 1.0 döner
WEATHER_API_KEY=your_openweathermap_api_key

# Fiyatlandırma Parametreleri
FIYAT_KM_BASI=5.00
FIYAT_YAGMUR_CARPANI=1.30
FIYAT_GECE_EK_UCRET=15.00
FIYAT_GECE_BASLANGIC=22
FIYAT_MIN_UCRET=20.00
